Tapşırıq 1

Ekranda `"Salam Javascript"` yazısını göstərin.

Tapşırıq 2

`ad` adlı dəyişən təyin edin, daxilinə öz adınızı yazın və ekranda göstərin.

Tapşırıq 3

`let` və `const` istifadə edərək iki fərqli dəyişən təyin edin və fərqlərinə baxın

Tapşırıq 4

`deyer` adlı dəyişən təyin edin və içərisinə fərqli tipdə məlumatlar və hər dəfə onun tipini `typeof` ilə ekranda göstərin.

Tapşırıq 5

Dəyəri `undefined` və `null` olan iki fərqli dəyişən təyin edin və onları ekranda göstərin.

Tapşırıq 6

İstifadəçidən `prompt()` vasitəsilə yaşını soruşun və daxil edilən dəyəri ekranda göstərin.

Tapşırıq 7

`eded1` və `eded2` dəyişənlərini toplayın və nəticəni ekranda göstərin.

**JavaScript**

```
const eded1 = 15;
const eded2 = 25;
```

Tapşırıq 8

Verilmiş ədədlərin çıxma nəticəsini ekranda göstərin.

**JavaScript**

```
const a = 50;
const b = 20;
```

Tapşırıq 9

Verilmiş ədədlərin vurma nəticəsini ekranda göstərin.

**JavaScript**

```
const x = 6;
const y = 7;
```

Tapşırıq 10

`bolme` dəyişəninə `a` və `b` ədədlərinin bölməsini təyin edin və ekranda göstərin.

**JavaScript**

```
const a = 100;
const b = 4;
```

Tapşırıq 11

`qaliq` dəyişəninə `x`-in `y`-ə bölməsindən qalan qalığı təyin edin və ekranda göstərin.

**JavaScript**

```
const x = 15;
const y = 4;
```

Tapşırıq 12

`eded` dəyişəninin dəyərini 3-cü qüvvətə yüksəldin və ekranda göstərin.

**JavaScript**

```
const eded = 5;
```

Tapşırıq 13

`say` dəyişəninin dəyərini `++` operatoru ilə 1 vahid artırın və ekranda göstərin.

**JavaScript**

```
let say = 10;
```

Tapşırıq 14

`balans` dəyişəninin dəyərini `--` operatoru ilə 1 vahid azaldın və ekranda göstərin.

**JavaScript**

```
let balans = 100;
```

Tapşırıq 15

`+=` operatorundan istifadə edərək `x` dəyişəninin üzərinə 5 əlavə edin və ekranda göstərin.

**JavaScript**

```
let x = 20;
```

Tapşırıq 16

`ad` və `soyad` dəyişənlərini `+` operatoru ilə birləşdirin (concatenation) və ekranda göstərin.

**JavaScript**

```
const ad = "Elnur";
const soyad = "Məmmədov";
```

Tapşırıq 17

`kursAdı` dəyişənini tick (backtick ```) işarələrindən istifadə edərək cümlə daxilində ekranda göstərin.

**JavaScript**

```
const kursAdi = "JavaScript";
// Gözlənilən mətn: "Mən JavaScript kursu keçirəm"
```

Tapşırıq 18

`yas` dəyişəni 18-dən böyükdürsə ekranda `"Giriş icazəlidir"`, əks halda `"Giriş qadağandır"` sözünü göstərən şərt yazın.

**JavaScript**

```
const yas = 20;
```

Tapşırıq 19

`yas` dəyişənini yoxlayan ternary operator yazın. Doğrudursa ekranda `"Keçdi"`, yanlışdırsa `"Qaldı"` göstərin.

**JavaScript**

```
const xal = 65;
```

Tapşırıq 20

`gun` dəyişəninə uyğun olaraq `switch` statement yazın. Case 1 olarsa ekranda `"Bazar ertəsi"`, default halda `"Digər gün"` göstərin.

**JavaScript**

```
const gun = 1;
```

Tapşırıq 21

`a` və `b` dəyişənlərini `===` operatoru ilə dəqiq yoxlayın və nəticəni ekranda göstərin.

**JavaScript**

```
const a = 5;
const b = "5";
```

Tapşırıq 22

`a` və `b` dəyişənlərinin bir-birinə dəqiq bərabər olmadığını `!==` operatoru ilə yoxlayın və ekranda göstərin.

**JavaScript**

```
const a = 10;
const b = 20;
```

Tapşırıq 23

`a` dəyişəninin `b`-dən böyük və ya bərabər (`>=`) olduğunu yoxlayın və ekranda göstərin.

**JavaScript**

```
const a = 15;
const b = 15;
```

Tapşırıq 24

`yas` dəyişəninin 18-dən böyük VƏ (`&&`) `bilet` dəyişəninin `true` olduğunu yoxlayan şərt yazın. Doğrudursa ekranda `"Keçə bilərsiniz"` göstərin.

**JavaScript**

```
const yas = 22;
const bilet = true;
```

Tapşırıq 25

`istifadeciAdi` VƏ YAXUD (`||`) `epoct` dəyişənlərindən hər hansı birinin dolu olduğunu yoxlayın. Doğrudursa ekranda `"Giriş olundu"` göstərin.

**JavaScript**

```
const istifadeciAdi = "";
const epoct = "test@mail.com";
```

Tapşırıq 26

`deyer` dəyişəninin `null` və ya `undefined` olub-olmadığını `??` (nullish coalescing) operatoru ilə yoxlayın, boşdursa ekranda `"Defolt dəyər"` göstərin.

**JavaScript**

```
let deyer = null;
```

Tapşırıq 27

Ekranda `"Salam"` yazısı göstərən `salamla` adlı funksiya təyin edin və onu çağırın.

Tapşırıq 28

`ad` parametri qəbul edən və onu ekranda göstərən `istifadeciSalamla` funksiyası təyin edin.

Tapşırıq 29

`ad` və `mesaj` parametrləri qəbul edən `mesajGonder` funksiyası təyin edin. `mesaj` parametri boş buraxılarsa defolt dəyəri `"Salam"` olsun.

Tapşırıq 30

İki ədədi parametr kimi qəbul edən və onları toplayıb ekranda göstərən `topla` adlı arrow function təyin edin.

Tapşırıq 31

`reqemler` adlı array təyin edin, daxilinə 1-dən 5-ə qədər ədədlər yazın və ekranda göstərin.

Tapşırıq 32

Verilmiş array-in 0-cı və 2-ci indeksindəki elementlərini ekranda göstərin.

**JavaScript**

```
const meyveler = ["Alma", "Armud", "Albalı"];
```

Tapşırıq 33

Verilmiş array-in daxilində neçə element olduğunu `length` mülkiyyəti ilə ekranda göstərin.

**JavaScript**

```
const seherler = ["Bakı", "Gəncə", "Sumqayıt"];
```

Tapşırıq 34

Verilmiş array-in sonuncu elementini `arr[arr.length - 1]` üsulu ilə ekranda göstərin.

**JavaScript**

```
const rengler = ["Qırmızı", "Yaşıl", "Mavi"];
```

Tapşırıq 35

Verilmiş array-in sonuncu elementini `.at(-1)` metodu ilə ekranda göstərin.

**JavaScript**

```
const heyvanlar = ["Pişik", "İt", "Quş"];
```

Tapşırıq 36

İkiölçülü (nested) array-in daxilindəki `4` ədədini ekranda göstərin.

**JavaScript**

```
const matris = [[1, 2], [3, 4]];
```

Tapşırıq 37

Array destructuring istifadə edərək `a` və `b` dəyişənlərini təyin edin və ekranda göstərin.

**JavaScript**

```
const koordinatlar = [10, 20];
```

Tapşırıq 38

`ad`, `yas` və `ixtisas` key-ləri olan `telebe` adlı obyekt təyin edin və ekranda göstərin.

Tapşırıq 39

Obyektin daxilindəki `model` dəyərini dot notation (`.`) vasitəsilə ekranda göstərin.

**JavaScript**

```
const masin = { marka: "Toyota", model: "Corolla" };
```

Tapşırıq 40

Obyektin daxilindəki `qiymet` dəyərini bracket notation (`[]`) vasitəsilə ekranda göstərin.

**JavaScript**

```
const telefon = { ad: "iPhone", qiymet: 2000 };
```
